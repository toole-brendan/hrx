{
  "name": "handreceipt",
  "version": "1.0.0",
  "description": "Military-grade property accountability system",
  "scripts": {
    "db:generate": "cd sql && drizzle-kit generate",
    "db:migrate": "cd sql && drizzle-kit migrate",
    "db:push": "cd sql && drizzle-kit push",
    "db:studio": "cd sql && drizzle-kit studio",
    "db:drop": "cd sql && drizzle-kit drop"
  },
  "devDependencies": {
    "@types/node": "^22.10.6",
    "drizzle-kit": "^0.29.1",
    "drizzle-orm": "^0.38.3",
    "drizzle-zod": "^0.5.1",
    "zod": "^3.24.1"
  }
}
