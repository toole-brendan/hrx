import React from 'react';
import { Badge } from '@/components/ui/badge';
import { cn } from '@/lib/utils'; interface StatusBadgeProps { status:"pending" |"approved" |"rejected" |"active" |"transferred"; className?: string;
} const StatusBadge: React.FC<StatusBadgeProps> = ({ status, className }) => { const statusConfig = { pending: { label: 'Pending', classes: 'bg-yellow-100 text-yellow-800' }, approved: { label: 'Approved', classes: 'bg-green-100 text-green-800' }, rejected: { label: 'Rejected', classes: 'bg-red-100 text-red-800' }, active: { label: 'Active', classes: 'bg-blue-100 text-blue-800' }, transferred: { label: 'Transferred', classes: 'bg-purple-100 text-purple-800' } }; const config = statusConfig[status]; return ( <Badge variant="outline" className={cn(config.classes, className)} > {config.label} </Badge> );
}; export default StatusBadge;