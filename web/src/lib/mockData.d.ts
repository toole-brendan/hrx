import { Property, Transfer, Activity, Notification } from "@/types";
export declare const inventory: Property[];
export declare const transfers: Transfer[];
export declare const activities: Activity[];
export declare const notifications: Notification[];
